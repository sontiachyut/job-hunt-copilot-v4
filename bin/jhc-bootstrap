#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)
PROJECT_ROOT=$(cd -- "${SCRIPT_DIR}/.." && pwd)
PYTHON_BIN=${JHC_PYTHON_BIN:-python3}

exec "${PYTHON_BIN}" -m job_hunt_copilot.bootstrap --project-root "${PROJECT_ROOT}" "$@"
